/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/psicoterapeutas',
  images: { unoptimized: true },
  trailingSlash: true,
};

export default nextConfig;
